# ── Frontend (Vite) ──────────────────────────────────────────
# URL бекенду — при локальному запуску
VITE_API_URL=http://localhost:8000/api

# При запуску через Docker — замінити на:
# VITE_API_URL=http://localhost:8000/api